"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function findPullRequestId(message) {
    var lines = message.split("\n");
    var firstLine = lines[0];
    var mergeMatch = firstLine.match(/^Merge pull request #(\d+) from /);
    if (mergeMatch) {
        return mergeMatch[1];
    }
    var squashMergeMatch = firstLine.match(/\(#(\d+)\)$/);
    if (squashMergeMatch) {
        return squashMergeMatch[1];
    }
    var homuMatch = firstLine.match(/^Auto merge of #(\d+) - /);
    if (homuMatch) {
        return homuMatch[1];
    }
    return null;
}
exports.default = findPullRequestId;
